<Chapter Label="Introduction">
<Heading>Introduction</Heading>
The <C>AutomGrp</C> package provides methods for computation with groups and
semigroups generated by finite automata or given by wreath recursions, as well
as with their finitely generated subgroups, subsemigroups and elements.
<P/>
The project originally started in 2000 mostly for personal use.
It was gradually expanding during consequent years, including both
addition of new algorithms and simplification of user interface. It
was used in the process of classification of groups generated by
<M>3</M>-state automata over a 2-letter alphabet (see
<Cite Key="BGK32"/>), as well as many subsequent papers.
<P/>
First author thanks Sveta and Max Muntyan for their infinite patience and
understanding. Second author thanks Olga, Anna, Irina and Andrey Savchuk for their help
and understanding. This project would be impossible without them.
<P/>
We would like to express our warm gratitude to Rostislav Grigorchuk, Zoran Sunic,
Volodymyr Nekrashevych, Ievgen Bondarenko, Rostyslav Kravchenko, Yaroslav and Maria
Vorobets and Ben Steinberg for their support, valuable comments, feature requests and constant interest
in the project.
<P/>
We also appreciate the code provided by Andrey Russev that was used to optimize the minimization of autmata function. Last, but not the least, we want to thank the anonymous referee for a very detailed review and numeruous constructive comments that significantly increased the quality of the accepted version of the package.
<P/>
Both authors were partially supported by NSF grants DMS-0600975, DMS-0456185 and DMS-0308985. The second author
appreciates the support from the New Researcher Grant from University of South Florida and the Simons
Collaboration Grant from Simons Foundation.
<Section Label="Short math background">
<Heading>Short math background</Heading>
This package deals mostly with groups acting on rooted trees. In
this section we recall necessary definitions and notation that will
be used throughout the manual. For more detailed introduction to the
theory of groups generated by automata we refer the reader
to&nbsp;<Cite Key="GNS00"/>.
<P/>
The infinite connected tree with selected vertex, called
the <A>root</A>, in which the degree of every vertex except the root is
<M>d+1</M> and the degree of the root is <M>d</M> is called the &lt;regular
homogeneous rooted tree of degree <M>d</M>&gt; (or &lt;<M>d</M>-ary
tree&gt;). The rooted tree of degree <M>2</M> is called the &lt;binary tree&gt;.
<P/>
The <M>n</M>-th <A>level</A> of the tree consists of all vertices located at
distance <M>n</M> from the root (here we mean combinatorial distance in
the graph).
<P/>
Similarly one defines &lt;spherically homogeneous&gt; (or <A>spherically-transitive</A>)
 rooted trees as rooted trees, such that the degrees of all vertices at each level
coincide (but may depend on the level).
<P/>
Given a finite alphabet <M>X=\{1,2,\ldots,d\}</M> the set <M>X^{*}</M> of all
finite words over <M>X</M> may be endowed with the structure of a <M>d</M>-ary
tree in which the empty word <M>\emptyset</M> is the <A>root</A>, the <A>level</A>
<M>n</M> in <M>X^{*}</M> consists of the words of length <M>n</M> over <M>X</M> and
every vertex <M>v</M> has <M>d</M> children, labeled by <M>vx</M>, for <M>x\in X</M>.
<P/>
Any automorphism <M>f</M> of a rooted tree <M>T</M> fixes the root and the
levels. For any vertex <M>v</M> of the tree <M>T</M> each automorphism <M>f</M> induces the
automorphism <M>f|_v</M> of the subtree of <M>T</M> hanging down from the vertex <M>v</M> by
<M>f|_v(u)=w</M> if <M>f(vu)=v'w</M> for some <M>v'\in X^{|v|}</M> from the same
level as <M>v</M> (here  <M>|v|</M> denotes the combinatorial distance from <M>v</M> to
the root of the tree). This automorphism is called the <A>section</A> of <M>f</M> at
<M>v</M>.
<P/>
If the tree <M>T</M> is regular, then the subtrees hanging down from
vertices of <M>T</M> are canonically isomorphic to <M>T</M> and, thus, the
sections of any automorphism <M>f</M> of <M>T</M> can be considered as
automorphisms of <M>T</M> again.
<P/>
A group <M>G</M> of automorphisms of a regular rooted tree <M>T</M> is called
<A>self-similar</A> if all sections of every element of <M>G</M> belong to
<M>G</M>.
<P/>
A self-similar group <M>G</M> is called <A>contracting</A>
if there is a finite set <M>N</M> of elements of <M>G</M>, such that for any
<M>g</M> in <M>G</M> there is a level <M>n</M> such that all sections of <M>g</M> at
vertices of levels bigger than <M>n</M> belong to <M>N</M>. The smallest set
with such a property is called the <A>nucleus</A> of <M>G</M>.
<P/>
Any automorphism <M>f</M> of a rooted tree can be decomposed as
<Display>f=(f_1,f_2,\ldots,f_d)\sigma,</Display>
<P/>
where <M>f_1,\ldots,f_d</M> are the sections of <M>f</M> at the vertices of
the first level and <M>\sigma</M> is the permutation which permutes the subtrees
hanging down from these vertices.
<P/>
This notation is very convenient for performing multiplication of
elements. Throughout this manual and everywhere in the package we use the right
action of (semi)groups acting on trees and for automorphisms (homomorphisms) of
a tree <M>f</M> and <M>g</M>, the composition <M>f.g</M> means that <M>f</M> acts first. This is
consistent with the order of multiplication of permutations in &GAP; , even though
some authors in the field prefer to use the left action. With this convention
in mind, If <M>f=(f_1,f_2,\ldots,f_d)\sigma</M> and
<M>g=(g_1,g_2,\ldots,g_d)\pi</M>, then
<P/>
<Display>f.g=(f_1.g_{\sigma(1)},\ldots,f_d.g_{\sigma(d)})\sigma\pi,</Display>
<P/>
<Display>f^{-1}=(f_{\sigma^{-1}(1)}^{-1},\ldots,f_{\sigma^{-1}(d)}^{-1})\sigma^{-1}.</Display>
<P/>
The group of automorphisms of a rooted tree is said to be
<A>level-transitive</A> if it acts transitively on each level of the
tree.
<P/>
Everything above applies also for homomorphisms of rooted trees
(maps preserving the root and incidence relation of the vertices).
The only difference is that in this case we get semigroups and
monoids of tree homomorphisms.
<P/>
A special class of self-similar groups is the class of groups
generated by finite automata. This class is especially nice from
the algorithmic point of view. Let us recall basic definitions.
<P/>
A &lt;Mealy automaton&gt; (<A>transducer</A>, &lt;synchronous automaton&gt;, or,
simply, <A>automaton</A>) is a tuple <M>A=(Q,X,\rho,\tau)</M>, where <M>Q</M> is
a set of <A>states</A>, <M>X</M> is a finite <A>alphabet</A> of cardinality <M>d \geq
2</M>, <M>\rho:Q \times X \to X</M> is a map, called the &lt;output map&gt;, <M>\tau:Q
\times X \to Q</M> is a map, called the &lt;transition map&gt;.
<P/>
If for each state <M>q</M> in <M>Q</M>, the restriction <M>\rho_q: X \to X</M>
given by <M>\rho_q(x)=\rho(q,x)</M> is a permutation, the automaton is
called <A>invertible</A>.
<P/>
If the set <M>Q</M> of states is finite, the automaton is called
<A>finite</A>.
<P/>
If some state <M>q</M> in <M>Q</M> of the automaton <M>A</M> is selected to be initial,
the automaton is called <A>initial</A> and denoted <M>A_q</M>. If an initial
state is not specified, the automaton is called <A>noninitial</A>.
<P/>
An initial automaton naturally acts on <M>X^{*}</M> by homomorphisms
(automorphisms in the case of an invertible automation) in the following way.
Given a word
<M>x_1x_2\ldots x_n</M>, the automaton starts at the initial state <M>q</M>,
reads the first input letter <M>x_1</M>, outputs the letter <M>\rho_q(x_1)</M>
and changes its state to <M>q_1=\tau(q,x_1)</M>. The rest of the input
word is handled by the new state <M>q_1</M> in the same way. Formally
speaking, the functions <M>\rho</M> and <M>\tau</M> can be extended to <M>\rho\colon Q
\times X^{*} \to X^{*}</M> and <M>\tau\colon Q \times X^{*} \to Q</M>.
<P/>
Given an automaton <M>A</M> the group <M>G(A)</M> of automorphisms of <M>X^{*}</M>
generated by all the states of <M>A</M> (as initial automata) is called the
&lt;automaton group&gt; defined by <M>A</M>.
<P/>
Every automaton group is self-similar, because the section of <M>A_q</M>
at vertex <M>v</M> is just <M>A_{\tau(q,v)}</M>.
<P/>
A special case is the case of groups generated by finite automata
and their subgroups. In this class we can solve the word problem,
which makes it much nicer from the computational point of view.
<P/>
Finite automata are often described by &lt;recursive relations&gt; (or &lt;wreath recursion&gt;) of
the form
<P/>
<Display>q=(\tau(q,1),\ldots,\tau(q,d)) \rho_q</Display>
<P/>
for every state <M>q</M>. For example, the line <M>a=(a,b)(1,2), b=(a,b)</M>
describes the automaton with 2 states <M>a</M> and <M>b</M>, such that <M>a</M>
permutes the letters <M>1</M> and <M>2</M> of the alphabet <M>X=\{1,2\}</M> and <M>b</M>
does not; and, independently
of the current state, the automaton changes its initial state to <M>a</M> if
it reads <M>1</M> and to <M>b</M> if it reads <M>2</M>. This particular automaton
generates the, so-called, lamplighter group.
<P/>
Semigroups generated by noninvertible automata are defined in a similar way.
</Section>
<Section Label="Installation instructions">
<Heading>Installation instructions</Heading>
<C>AutomGrp</C> package requires &GAP; version at least 4.4.6 and <C>FGA</C> (Free
Group Algorithms) package available at <URL>https://www.gap-system.org/Packages/fga.html</URL>
<P/>
The installation of the <C>AutomGrp</C> package follows the standard &GAP; rules, i.e.
to install it unpack the archive into the <C>pkg</C> directory of
your &GAP; distribution. This will create <C>automgrp</C> subdirectory.
<P/>
To load package issue the command
<Log><![CDATA[
gap> LoadPackage("automgrp");
----------------------------------------------------------------
Loading  AutomGrp 1.3 (Automata Groups and Semigroups)
by Yevgen Muntyan (muntyan@fastmail.fm)
   Dmytro Savchuk (http://savchuk.myweb.usf.edu/)
Homepage: https://gap-packages.github.io/automgrp/
----------------------------------------------------------------
true
]]></Log>
Note, that if the <C>FR</C> package by Laurent Bartholdi is installed as well, &GAP; 
will automatically load it, together with the packages on which it depends. The <C>FR</C>
package functionality partially overlaps with the one of <C>AutomGrp</C>. For the
user's convenience and to expand the functionality of both packages, several converters (see operations <Ref Func="AutomGrp2FR"/>
and <Ref Func="FR2AutomGrp"/>) of the basic data types were implemented in <C>AutomGrp</C>.
<P/>
To test the installation, issue the command
<Log><![CDATA[
gap> Read( Filename( DirectoriesPackageLibrary( "automgrp", "tst" ), "testall.g"));
]]></Log>
in the &GAP; command line.
</Section>
<Section Label="Quick example">
<Heading>Quick example</Heading>
Here is how to define the Grigorchuk group and Basilica group.
<Example><![CDATA[
gap> Grigorchuk_Group := AutomatonGroup("a=(1,1)(1,2),b=(a,c),c=(a,d),d=(1,b)");
< a, b, c, d >
gap> Basilica := AutomatonGroup( "u=(v,1)(1,2), v=(u,1)" );
< u, v >
]]></Example>
Similarly one can define a group (or semigroup) generated by
a noninvertible automaton. As an example we consider the semigroup of
intermediate growth generated by the two state automaton
(<Cite Key="BRS06"/>)
<Example><![CDATA[
gap> SG := AutomatonSemigroup( "f0=(f0,f0)(1,2), f1=(f1,f0)[2,2]" );
< f0, f1 >
]]></Example>
Another type of groups (semigroups) implemented in the package is
the class of groups (semigroups) defined by wreath recursion
(finitely generated self-similar groups).
<Example><![CDATA[
gap> WRG := SelfSimilarGroup("x=(1,y)(1,2),y=(z^-1,1)(1,2),z=(1,x*y)");
< x, y, z >
]]></Example>
Now we can compute several properties of <C>Grigorchuk_Group</C>, <C>Basilica</C> and <C>SG</C>
<Example><![CDATA[
gap> IsFinite(Grigorchuk_Group);
false
gap> IsSphericallyTransitive(Grigorchuk_Group);
true
gap> IsFractal(Grigorchuk_Group);
true
gap> IsAbelian(Grigorchuk_Group);
false
gap> IsTransitiveOnLevel(Grigorchuk_Group, 4);
true
]]></Example>
We can also check that <C>Basilica</C> and <C>WRG</C> are contracting and compute their nuclei
<Example><![CDATA[
gap> IsContracting(Basilica);
true
gap> GroupNucleus(Basilica);
[ 1, u, v, u^-1, v^-1, u^-1*v, v^-1*u ]
gap> IsContracting( WRG );
true
gap> GroupNucleus( WRG );
[ 1, y*z^-1*x*y, z^-1*y^-1*x^-1*y*z^-1, z^-1*y^-1*x^-1, y^-1*x^-1*z*y^-1, 
  z*y^-1*x*y*z, x*y*z ]
]]></Example>
The group <C>Grigorchuk_Group</C> is generated by a bounded automaton and, thus, is
amenable (see <Cite Key="BKNV05"/>)
<Example><![CDATA[
gap> IsGeneratedByBoundedAutomaton(Grigorchuk_Group);
true
gap> IsAmenable(Grigorchuk_Group);
true
]]></Example>
We can compute the stabilizers of levels and vertices
<Example><![CDATA[
gap> StabilizerOfLevel(Grigorchuk_Group, 2);
< c*b^-1, d, a^2, a*c*b^-1*a^-1, a*d*a^-1, b^2, b*c, b*d*b^-1, a*b^2*a^-1, a*b\
*c*a^-1, a*b*d*b^-1*a^-1, b*a^2*b^-1, b*a*c*b^-1*a^-1*b^-1, b*a*d*a^-1*b^-1, a\
*b*a^2*b^-1*a^-1, a*b*a*c*(b^-1*a^-1)^2, a*b*a*d*a^-1*b^-1*a^-1, (b*a)^2*(b^-1\
*a^-1)^2, b*a*b^2*a^-1*b^-1, b*a*b*c*a^-1*b^-1, b*a*b*d*b^-1*a^-1*b^-1, (a*b)^\
2*a*b^-1*a^-1*b^-1, (a*b)^2*b*a^-1*b^-1*a^-1, (a*b)^2*c*a^-1*b^-1*a^-1, (a*b)^\
2*d*(b^-1*a^-1)^2 >
gap> StabilizerOfVertex(Grigorchuk_Group, [2, 1]);
< b, c, d, a^2, a*c*b^-1*a^-1, a*d*a^-1, a*b^2*a^-1, a*b*c*a^-1, a*b*d*b^-1*a^\
-1, a*b*a^2*b^-1*a^-1, (a*b)^2*a^-1*b^-1*a^-1, a*b*a*c*a^-1*b^-1*a^-1, a*b*a*d\
*a^-1*b^-1*a^-1 >
]]></Example>
In the case of a finite group we can produce an isomorphism into a permutation group
<Example><![CDATA[
gap> f := IsomorphismPermGroup(Group(a,b));
MappingByFunction( < a, b >, Group([ (1,2)(3,5)(4,6)(7,9)(8,10)(11,13)(12,14)
  (15,17)(16,18)(19,21)(20,22)(23,25)(24,26)(27,29)(28,30)(31,32), (1,3)(2,4)
  (5,7)(6,8)(9,11)(10,12)(13,15)(14,16)(17,19)(18,20)(21,23)(22,24)(25,27)
  (26,28)(29,31)(30,32) ]), function( g ) ... end, function( b ) ... end )
gap> Size(Image(f));
32
]]></Example>
Here is how to find relations in <C>Basilica</C> between elements of length not greater than 5.
<Example><![CDATA[
gap> FindGroupRelations(Basilica, 6);
v*u*v*u^-1*v^-1*u*v^-1*u^-1
v*u^2*v^-1*u^2*v*u^-2*v^-1*u^-2
v^2*u*v^2*u^-1*v^-2*u*v^-2*u^-1
[ v*u*v*u^-1*v^-1*u*v^-1*u^-1, v*u^2*v^-1*u^2*v*u^-2*v^-1*u^-2, 
  v^2*u*v^2*u^-1*v^-2*u*v^-2*u^-1 ]
]]></Example>
Or relations in the subgroup <M>\langle p=uv^{-1}, q=vu\rangle</M>
<Example><![CDATA[
gap> FindGroupRelations([u*v^-1,v*u], ["p", "q"], 5);
q*p^2*q*p^-1*q^-2*p^-1
[ q*p^2*q*p^-1*q^-2*p^-1 ]
]]></Example>
Or relations in the semigroup <C>SG</C>
<Example><![CDATA[
gap> FindSemigroupRelations(SG, 4);
f0^3 = f0
f0^2*f1 = f1
f1*f0^2 = f1
f1^3 = f1
[ [ f0^3, f0 ], [ f0^2*f1, f1 ], [ f1*f0^2, f1 ], [ f1^3, f1 ] ]
]]></Example>
Some basic operations with elements are the following:
<P/>
The function <C>IsOne</C> computes whether an element represents the
trivial automorphism of the tree
<Example><![CDATA[
gap> IsOne( (a*b)^16 );
true
]]></Example>
Here is how to compute the order (this function might not stop in
some cases)
<Example><![CDATA[
gap> Order(a*b);
16
gap> Order(u^22*v^-15*u^2*v*u^10);
infinity
]]></Example>
Note that the package contains many helpful notifications that can be enabled
by changing <C>InfoLevel</C> for <C>InfoAutomGrp</C>. In many situations level 3 provides
additional information about the computation that can be used either to track the
progress or to extract the proof from the result as it can be done in the example
below.
<Example><![CDATA[
gap> SetInfoLevel(InfoAutomGrp,3);
gap> Order(u^22*v^-15*u^2*v*u^10);
#I  v is obtained from (u^22*v^-15*u^2*v*u^10)^1
    by taking sections and cyclic reductions at vertex [ 1, 1, 1, 1, 1, 1, 1, 1, 1 ]
#I  v is obtained from (v)^2
    by taking sections and cyclic reductions at vertex [ 1, 1 ]
infinity
gap> SetInfoLevel(InfoAutomGrp,0);
]]></Example>
One can check if a particular element acts spherically transitively on the tree
(this function might not stop in some cases)
<Example><![CDATA[
gap> IsSphericallyTransitive(a*b);
false
gap> IsSphericallyTransitive(u*v);
true
]]></Example>
The sections of an element can be obtained as follows
<Example><![CDATA[
gap> Section(u*v^2*u, 2);
u^2*v
gap> Decompose(u*v^2*u);
(v, u^2*v)
gap> Decompose(u*v^2*u, 3);
(v, 1, 1, 1, u*v, 1, u, 1)(1,2)(5,6)
]]></Example>
One can try to compute whether the elements of group <C>WRG</C> defined
by wreath recursion are finite-state and calculate corresponding
automaton
<Example><![CDATA[
gap> IsFiniteState(x*y^-1);
true
gap> AllSections(x*y^-1);
[ x*y^-1, z, 1, x*y, y*z^-1, z^-1*y^-1*x^-1, y^-1*x^-1*z*y^-1, z*y^-1*x*y*z, 
  y*z^-1*x*y, z^-1*y^-1*x^-1*y*z^-1, x*y*z, y, z^-1, y^-1*x^-1, z*y^-1 ]
gap> A := MealyAutomaton(x*y^-1);
<automaton>
gap> NumberOfStates(A);
15
]]></Example>
To get the action of an element on a vertex or on a particular level of the tree
use the following commands
<Example><![CDATA[
gap> [1,2,1,1]^(a*b);
[ 2, 2, 1, 1 ]
gap> PermOnLevel(u*v^2*v, 3);
(1,6,4,8,2,5,3,7)
]]></Example>
The action of the whole group <C>Grigorchuk_Group</C> on some level can be computed via
<Ref Func="PermGroupOnLevel"/>.
<Example><![CDATA[
gap> PermGroupOnLevel(Grigorchuk_Group, 3);
Group([ (1,5)(2,6)(3,7)(4,8), (1,3)(2,4)(5,6), (1,3)(2,4), (5,6) ])
gap> Size(last);
128
]]></Example>
The next example shows how to find all elements of length at most 5 of order 16 in the Grigorchuk group.
<Example><![CDATA[
gap> FindElements(Grigorchuk_Group, Order, 16, 5);
[ a*b, b*a, c*a*d, d*a*c, a*b*a*d, a*c*a*d, a*d*a*b, a*d*a*c, b*a*d*a, 
  c*a*d*a, d*a*b*a, d*a*c*a, a*c*a*d*a, a*d*a*c*a, (b*a)^2*c, b*(a*c)^2, 
  c*(a*b)^2, (c*a)^2*b ]
]]></Example>
</Section>
</Chapter>
